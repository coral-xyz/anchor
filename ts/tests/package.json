{
  "name": "@project-serum/tests",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "rimraf dist && tsc -p . && ts-node dist"
  },
  "devDependencies": {
    "@types/node": "*",
    "rimraf": "*",
    "ts-node": "*",
    "typescript": "*"
  },
  "dependencies": {
    "@project-serum/anchor": "*",
    "@solana/web3.js": "*"
  }
}
