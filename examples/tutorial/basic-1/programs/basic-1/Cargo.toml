[package]
name = "basic-1"
version = "0.1.0"
description = "Created with Anchor"
rust-version = "1.56"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "basic_1"

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
test-bpf = [ "solana-validator" ]

[dependencies]
anchor-lang = { path = "../../../../../lang" }
solana-validator = { version = "1.9.14", optional = true }

[dev-dependencies]
solana-program-test = "1.9.14"
solana-sdk = "1.9.14"
